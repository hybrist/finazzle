# Progress Log

## 2026-04-04
- Added gitignored `data/` staging area README and updated `.gitignore`; ran `./check` and closed finazzle-2w0.
- Implemented the Shadow Register SQLite schema plus `finazzle shadow import`; documented usage, added importer tests, ran `./check`, and closed finazzle-514.
- Built the transfer matching engine + `finazzle shadow match` report, covering CLI + matcher tests, ran `./check`, and prepared to close finazzle-9x3.
- Crafted fabricated Chase test cases covering normalization -> `shadow import` -> `shadow match`, documented the fixtures, ran `./check`, and closed finazzle-bw9.

## 2026-04-05
- Added the CSV normalization layer + `--mapping` option for `finazzle shadow import`, covering normalizer module, importer wiring, docs, and tests; ran `./check` and closed finazzle-846.
