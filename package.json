{
  "name": "finazzle",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first cash flow tooling monorepo",
  "license": "MIT",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "cli": "pnpm --filter @finazzle/cli run dev"
  }
}
